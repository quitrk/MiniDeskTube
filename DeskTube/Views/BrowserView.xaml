<UserControl x:Class="DeskTube.Views.BrowserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <Grid>
        <WebBrowser x:Name="browser" Height="400" />
        <Popup PlacementTarget="{Binding ElementName=browser}" Placement="Center" IsOpen="{Binding IsOverlayActive}" StaysOpen="True" AllowsTransparency="True" x:Name="BrowserOverlay">
            <Border Background="#11000000" 
                    Width="{Binding ActualWidth, ElementName=browser}"
                    Height="{Binding ActualHeight, ElementName=browser}"
                    Visibility="{Binding Visibility, ElementName=browser}"/>
        </Popup>
    </Grid>
</UserControl>
